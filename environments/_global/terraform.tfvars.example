azure_subscription_id = "00000000-0000-0000-0000-000000000000" # TODO(deploy): real connectivity/global subscription GUID
subscription_code     = "conn"
environment           = "prd" # TDA §4 uses "prd" (not "prod")
location              = "uksouth"

mandatory_tags = {
  cost_centre         = "CC-4821"                  # TODO(deploy): real charge-back cost centre
  owner               = "avd-platform@example.com" # TODO(deploy): real accountable owner (team DL/email)
  data_classification = "Internal"                 # TODO(deploy): confirm classification (e.g. Public/Internal/Confidential)
  service_criticality = "Gold"                     # TODO(deploy): confirm criticality tier
}
